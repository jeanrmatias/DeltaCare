"""Infraestrutura: banco de dados, senha, sessão e gravação de arquivos.

Aqui mora o que não é regra de negócio — o que o sistema *usa* para funcionar,
e não o que ele *decide*. As regras ficam em `regras/`.
"""
