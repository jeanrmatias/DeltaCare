"""Regras de negócio do Delta Care.

Nenhum módulo aqui importa FastAPI. É o que permite testar as regras direto,
sem subir servidor (ver testes.py), e o que permitiria trocar a camada HTTP
sem reescrever o que o sistema decide.
"""
